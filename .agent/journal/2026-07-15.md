- Investigated hub follow-up: operator saw the "undefined" rendering bug again after the 2026-07-14 fix (7bf029e). Reproduced the operator's exact report sentence (numbers, em-dash, arrow) against this repo's `renderMarkdown` in a vm sandbox — it rendered clean, no "undefined". Root cause was not a remaining code defect: the operator's actual running hub instance (a separate `git clone` of this repo, not this working copy) was still serving a pre-7bf029e copy of `control-plane/public/app.js` — the fix had never been deployed to it. Patched that live instance's `app.js` directly (identical 2-line change, byte-for-byte matches this repo's fixed version now) since it's served fresh from disk per request with no server restart required.
- Also found `54c0dcd` (registry.json hot-reload) was committed but never pushed, despite the prior journal entry claiming it as done — confirmed and pushed both it and a new regression-test commit (`df9f6e9`) to origin.
- Added a regression case to `tests/test-control-plane.sh` covering the operator's exact repro shape (multi-number report sentence with em-dash/arrow punctuation) so this can't silently regress again.
- Broader finding, not fixed here: diffing the live hub instance's `control-plane/` against this repo shows `server.js`, `lib/config.js`, and `lib/dispatcher.js` have also diverged — the live instance is missing the registry hot-reload wiring and the dispatcher's model-name shell-injection guard. These require a process restart to take effect, unlike the static `app.js` fix, so I did not apply them unilaterally to a live system. Flagged to hub as a deployment-sync gap needing a deliberate decision, not silently patched.

Challenge: the bug report looked like a new/uncovered edge case ("number followed by word, no space"), but reproducing it against this repo's current code showed no bug at all — the actual gap was that "this repo's fix" and "what's actually running for the operator" are two different copies of the same file with no sync mechanism between them.

Tests: full suite (`tests/run-tests.sh`) — 11/11 passed, including the new regression case.

- Follow-up, operator approved: staged the flagged deployment-sync fix on the live hub instance (`/home/tmanso/bizagent`, this session's own working directory — it is itself a running installed copy of this repo). Copied `control-plane/server.js`, `control-plane/lib/config.js`, and `control-plane/lib/dispatcher.js` byte-for-byte from this repo's `origin/main` (`df9f6e9`, confirmed clean/up to date before copying) into that instance, matching the earlier `app.js` fix. Ran that instance's own `tests/run-tests.sh` (11/11 pass) and `node --check` on all three files afterward. Changes are `git add`ed there but not committed — left for hub/operator to decide on commit message and timing.
- Unlike `app.js` (read fresh per request), these three files are `require()`d once at process start, so the registry hot-reload wiring and the dispatcher shell-injection guard will NOT take effect until that hub instance's control-plane process is restarted. Flagged clearly in the reply to hub; did not restart it myself.

- Hub follow-up: operator confirmed the deployment-sync fix works after the live instance's restart. Committed the staged files there as `dff12e9` (verified byte-identical to this repo's `df9f6e9` beforehand). That checkout has no git remote configured, so the commit stays local to that instance — flagged to hub that push must happen from wherever the operator's real GitHub push runs. bizagent-public itself needed no further action; it was already clean/pushed.
