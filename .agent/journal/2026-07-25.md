# 2026-07-25 | bizagent-public

- `edf0ea5` Fix control-plane.sh losing the live process after restart.
  - Shell now discovers running server via `/proc` cmdline (`serve --hub`) and the systemd user unit in addition to the pidfile; server writes its own pidfile on start and clears it on exit.
  - Prefers `systemctl --user` stop/start when unit is active to avoid Restart=on-failure respawn.
  - Challenge: systemd, bare node, nohup wrapper, and post-crash orphans all left only a launcher-owned pidfile.
  - Lesson: the long-lived process must own its pidfile; the wrapper must discover by cmdline and unit, never pidfile alone.

- `ce11041` Improve web UI chat sessions: ack copy, routing, delete, display name.
  - Launch-ack text changed to "Working. Stand by...".
  - Missing `conversation_id` on hub→user mail now prefers the originating in-flight turn (from `pending-hub-turns.json`) before falling back to the currently open tab.
  - Added `DELETE /api/conversations/:id` + confirmed UI delete button.
  - New profile API + `.bizagent/profile.json` for operator display name used in chat bubbles and session labels.

- `34b7280` Fix console stuck on interim launch-ack status.
  - `pollConversation` now advances on `updated_at` + last-message identity (`conversationPollStamp`), not just `messages.length`.
  - Same-length supersede (launch-ack replaced by real reply) now re-renders the open thread without requiring a conversation switch.

- `b77d0b9` Make wrong-channel hub replies structurally impossible.
  - CP creates a reserved `.bizagent/pending-replies/<conversation_id>.body.md` for console hub turns; model must write body only.
  - Hub replies must go through `scripts/write-message.sh` (or equivalent) with `--conversation-id`; free-form outbox creation is banned for operator/user mail.
  - Stdout is debug-only; on exit with no user-visible reply the safety net promotes from log or posts a hard in-UI error.
  - Challenge: prompt instructions alone never prevented the model from treating chat/stdout as the answer channel.
  - Lesson: the control plane must own the delivery artifact; success is the existence of the correctly addressed file.

- `6a69c46` Add write-message helper for reliable outbox mail.
  - Added `writeOutboxMessage` in `control-plane/lib/mail.js` for canonical filename + YAML frontmatter.
  - Exposed via `node scripts/bizagent-control-plane.js write-message` and the `scripts/write-message.sh` wrapper (supports `--hub` / `BIZAGENT_HUB`).

- `577bb31` CP: launch ack + hub outbox safety net for console turns.
  - On hub spawn with pending inbox that carries a `conversation_id`, CP immediately posts a deterministic launch-ack status into the console conversation before the CLI boots.
  - On CLI exit, shell EXIT trap + Node backup + tick drain check whether a user-visible reply was produced; if not, promote the last assistant blob from the dispatch log (or emit a clear hard error) into the outbox with the conversation id.
- `49651d7` (branch feat/makeover-port-readme-2026-07-25) Hub→public makeover port (Phases A/B) + README refresh.
  - Allowlist-only port of control-plane/, scripts/ (incl. hub-daemon, prune-archives, write-message), templates/, install/, docs/, tests additions, NIGHTLY/WEEKLY, and key root files.
  - publish-check clean; full suite 12/12 green.
  - README updated to document warm daemon + cold fallback, reserved reply body + write-message path, .bizagent/env, tuning, pruning, slot pools, and current layout. No private data.
  - Commit on feature branch; ready for review/push.
