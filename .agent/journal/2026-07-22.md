# 2026-07-22 | bizagent-public

- `9bb6e82` — Fix escape character in printf statement in `install.sh` (upstream/remote).
- `7dc024d` — Web UI: render GFM tables, safe links, and images in chat markdown. Extended `renderMarkdown`/`renderInline` in `control-plane/public/app.js` for tables and `![alt](url)`; links keep `target=_blank` + `rel=noopener noreferrer`; table/img CSS in `styles.css`; smoke coverage in `tests/test-control-plane.sh`.

Also on the line earlier this maintenance window (prior commits already on main): CLI migration series through hub `cliName` fix (`004f255` … `08d3b34`).

Challenge: chat used a minimal custom markdown renderer that left tables as plain text and ignored image syntax.
Lesson: keep renderer tests next to UI features that operators actually paste into chat (tables, links, images).
