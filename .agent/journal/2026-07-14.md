- Verified/regression-tested the "numbers render as literal 'undefined'" bug: root cause was already fixed in commit 7bf029e (control-plane/public/app.js) — the old codespan placeholder ` N ` (space-digit-space) collided with any bare number in message text, so `codeSpans[N]` came back `undefined` and got stitched into `<code>undefined</code>`. That commit swapped in a `\x00CODESPAN:N\x00` marker that can't appear in normal text. Added a regression test (tests/test-control-plane.sh) that loads app.js in a vm sandbox and asserts `renderMarkdown` never emits "undefined" for bare-number and number-near-codespan inputs, so this can't silently regress.
- Fixed registry.json hot-reload: control-plane/lib/config.js's `loadRuntimeConfig` only read registry.json once at process start, so `config.registry` (agent list, dispatch settings, model overrides) stayed frozen for the life of the server — new agents added to registry.json never showed up in `/api/state` or got dispatched until a manual restart (lib/mail.js's router already re-read the registry per call, so mail delivery worked, but the UI agent list and dispatcher didn't). Added `refreshRegistry(config)` (mtime-gated, single stat call) and call it at the top of `handleApi` and `runTick` in control-plane/server.js. Added a regression test proving a newly-added agent and a changed `dispatch.max_concurrency` are picked up without restart.

Challenge: bug #1 turned out to already be fixed by an earlier same-day commit with no regression test attached, so most of the work there was confirming the fix actually holds (via a vm-sandboxed functional test of app.js) rather than re-patching.

Tests: full suite (`tests/run-tests.sh`) — 11/11 passed, including the two new regression tests added to test-control-plane.sh.

Follow-up (later same day, responding to hub bug report on the same two issues): the fix above was implemented and tested but never committed — working tree still had it as uncommitted changes. Re-verified both fixes hold (re-ran full suite, 11/11; manually exercised hot-reload end-to-end against a scratch hub: added an agent to registry.json while the server was running, confirmed it appeared in `/api/state` without a restart) and committed as 54c0dcd. Not pushed (no push requested).
