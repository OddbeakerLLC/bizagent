# 2026-07-26 | bizagent-public

- `49651d7` feat: hub→public makeover port (Phases A/B) + README refresh.
  - Allowlist-only port of control-plane/, scripts/ (hub-daemon, prune-archives, write-message, control-plane.sh, nightly.sh etc.), templates/, install/, docs/, tests/, NIGHTLY/WEEKLY, and key root files (README, .gitignore, registry.example, etc.) from the live hub.
  - publish-check clean; full suite 12/12 green (including new test-makeover-phase1.sh).
  - README.md refreshed to document warm daemon + cold fallback, reserved reply body + mandatory write-message path, .bizagent/env, tuning, pruning, slot pools, observability, and updated layout. No private data.
  - Backfilled prior journal entries and added sitemap.md as part of the port.
  - Landed on main.

- `782b30e` fix: stamp agent→hub mail with conversation_id and post agent-completion notice.
  - Agent→hub mail is now stamped with the originating (or active) conversation_id.
  - On clean agent exit, when a hub slot is free, the dispatcher immediately routes the agent mail and nudges a hub dispatch for faster completion notification.
  - CP posts a deterministic "Agent <slug> finished — summarizing…" status message into the conversation (superseded by the real hub summary).
  - Hub runtime prompt and dispatch behavior updated to require a user-visible summary for stamped agent→hub mail.
  - New tests covering the stamp + notice path.

- `5691073` fix: prevent duplicate agent→hub completion notifications.
  - writeOutboxMessage now dedupes identical (from+to+body) messages.
  - postAgentCompletionNotice scans the conversation and refuses a second notice for the same agent slug.
  - routeOutboxes marks delivered agent→hub inbox files as dispatched immediately (prevents re-claim on fast nudge).
  - Hub prompt and dispatch.md.template hardened: "at most one" summary; archive after emitting the summary; exactly one outbox file per logical completion.
  - Added regression tests: one completion yields exactly one notice even under re-route/nudge or identical bodies.
  - Addresses at-least-once redispatch spam seen after agent-exit nudge + stamp.

Tests: 12/12 green after these changes.

- `967538e` fix: hub reply chat push when UI REST poll is off.
  - Root cause: hub-turn-safety / EXIT-hook / hub-daemon finalize conversation JSON in a **child** Node process (or after `syncUserInbox` already ran). That process cannot see main CP `wsConvClients` / SSE sets, so broadcast was a no-op; next tick found empty user inbox → no push; UI stuck until refresh.
  - Fix (main CP process only):
    - `setOnConversationMutated` → `pushConv` for in-process mutations (launch-ack, main-process drain/exit).
    - `drainHubTurnSafety` results → `conversationIdsFromSafetyResults` → `pushConv` after safety in `runTick`.
    - `pushConversationsChangedOnDisk` each tick (subscribed + active conv `updated_at` stamp) covers EXIT-hook / hub-daemon child mutations.
    - `readUserInboxMessages` returns `{relayed, ids}`; `syncUserInbox` pushes those ids.
  - UI: WS preferred, SSE only if WS down, REST poll last-resort / `?poll=1` / `BIZAGENT_UI_POLL=1`.
  - Also: package.json with `ws` dep; app.js WS-first boot; hub-turn-safety reserved-body push hooks; mail/conversations push-path helpers.
  - Live hub CP already restarted with the fix. Tests 12/12 green.
