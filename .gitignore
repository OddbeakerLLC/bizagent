# This is the PUBLIC framework repo. Never commit secrets or operator-specific data.
*.env
.env
*.key
*.pem
*_secret*
*secrets*

# Local scratch
*.log
*.lock

# Operational scaffolding must never land in the public framework repo.
# (Agent B keeps this product's journal in the operational hub, not here.)
.agent/
sitemap.md
